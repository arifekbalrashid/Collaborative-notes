name: Keep Render Alive

on:
  push:
    branches:
      - render-deploy
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render app
        run: |
          curl -s https://notes.arifekbalrashid.me/ping || echo "Ping failed"